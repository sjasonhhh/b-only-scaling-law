# Public data boundary

`B_scaling_laws/` contains the public B attachments used for reproducibility.

`evaluation_inputs/` contains feature-only inputs for feedback evaluation. It intentionally omits `val_loss`, `L_A_macro`, and all A domain Loss labels. Hidden labels, the private evaluator, and feedback history remain outside the public repository.
